# .htaccess LIMPIO PARA LOCAL (WAMP)
Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On
  # Mientras desarrollas, NO fuerces 301 globales:
  # Si quieres “fallback” a index.php, usa esto:
  # RewriteCond %{REQUEST_FILENAME} !-f
  # RewriteCond %{REQUEST_FILENAME} !-d
  # RewriteRule ^ index.php [L]   # o simplemente comenta todo si no lo necesitas
</IfModule>

# IMPORTANTE: NADA de AddHandler / SetHandler de cPanel aquí.
# WAMP ya tiene PHP integrado; no declares handlers personalizados.
